module.exports = {
    content: ["./ App.{ js, jsx, ts, tsx }”, “./ components/**/ *.{ js, jsx, ts, tsx }"], // Add any other directories containing code which will be displayed, such as the screens directory
    plugins: [], // Require any Tailwind CSS plugins I want to use, such as `require(“@tailwindcss/forms”)
};